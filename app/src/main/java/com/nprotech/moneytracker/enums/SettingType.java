package com.nprotech.moneytracker.enums;

public enum SettingType {

    ACCOUNT,
    WALLET,
    CURRENCY,
    MANAGE_CATEGORY,

    WEEK_STARTS_ON,
    STARTUP_SCREEN,
    LANGUAGE,
    PASSWORD,
    SMART_REMINDER,

    MANAGE_BACKUP,
    VERSION
}